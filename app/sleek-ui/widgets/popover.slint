import { UAppTheme } from "../app-theme.slint";
import { UCard } from "card.slint";


export component UPopover inherits PopupWindow {
    min-height: 10px;
    card := UCard {
        width: 100%;
        height: 100%;
        @children
    }
}
