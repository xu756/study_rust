import { UAppTheme } from "../app-theme.slint";


export component UText inherits Text {
	// Accessability properties
	accessible-role: text;
    accessible-value: self.text;
    color: UAppTheme.text;
    font-size: UAppTheme.font-size-base;
}
