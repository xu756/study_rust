import { UAppTheme } from "../app-theme.slint";


export component UIcon inherits Image {
    in property <length> size: UAppTheme.icon-size-base;
    colorize: UAppTheme.text;
    width: size;
    height: size;
    horizontal-alignment: center;
    vertical-alignment: center;
}
