

[target.aarch64-unknown-linux-gnu]
image = "docker.cnb.cool/xu756/xm/ghcr.io.cross-rs.aarch64-unknown-linux-gnu:0.2.5"
pre-build = [
    "dpkg --add-architecture $CROSS_DEB_ARCH",
    "apt-get update",
    "apt-get install --assume-yes libfontconfig1-dev:$CROSS_DEB_ARCH"
]
